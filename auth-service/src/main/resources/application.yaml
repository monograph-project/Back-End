spring:
  application:
    name: auth-service
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: native
  kafka:
    bootstrap-servers: http://localhost:9092
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI}
  cors:
    allowed-origins: ${SPRING_CORS_ALLOWED_ORIGINS:*}

keycloak:
  server-url: ${KEYCLOAK_SERVER_URL}
  realm: ${KEYCLOAK_REALM}
  admin:
    client-id: ${KEYCLOAK_ADMIN_CLIENT_ID}
    client-secret: ${KEYCLOAK_ADMIN_CLIENT_SECRET}
  public-client-id: ${KEYCLOAK_PUBLIC_CLIENT_ID}

google:
  client-id: ${GOOGLE_CLIENT_ID}
  client-secret: ${GOOGLE_CLIENT_SECRET}

app:
  default-role: ${APP_DEFAULT_ROLE:AUTHOR_USER}
  frontend-base-url: ${APP_FRONTEND_BASE_URL:http://localhost:5173}
  email-verification:
    otp-expiration-minutes: ${APP_EMAIL_VERIFICATION_OTP_EXPIRATION_MINUTES:15}
  app:
    jwt:
      secret: ${CUSTOME_SECRET}
